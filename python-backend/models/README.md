# Model checkpoints

Place runtime model files in this directory:

- `sam2.1_hiera_tiny.pt`
- `lama_fp32.onnx`

Checkpoint binaries are intentionally ignored by Git because of their size. Configure alternate paths with `SAM2_CHECKPOINT` and `LAMA_CHECKPOINT`.
